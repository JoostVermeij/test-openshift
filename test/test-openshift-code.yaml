apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  annotations:
    openshift.io/display-name: test-openshift-code
  labels:
    build: test-openshift-code
  name: test-openshift-code
  namespace: openshift-gitops
spec:
  lookupPolicy:
    local: true