apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: 'true'
  labels:
    app.kubernetes.io/instance: test
  name: myapp-service
  namespace: myapp
spec:
  host: myapp-service-myapp.apps-crc.testing
  port:
    targetPort: 8081
  to:
    kind: Service
    name: myapp-service
    weight: 100
  wildcardPolicy: None